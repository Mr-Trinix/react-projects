describe("Mathematic operations", () => {
    test("should return 2 if expect 1 + 1", () => {
        expect(1 + 1).toBe(2)
    })

    test("should return the same numbers", () => {
        expect(1).toBe(1)
    })

    test("should be 9 when make an addition", () => {
        expect(1 + 8).toBe(9)
    })
})